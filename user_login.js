function goToCategory(category) {
  // You can modify this logic to redirect to a category page
  alert("Navigating to " + category.toUpperCase() + " category");
  // Example: window.location.href = `${category}.html`;
}
